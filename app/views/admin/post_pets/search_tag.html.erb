<div class="container px-5 px-sm-0">
  <div class="row">

    <div class="col">
      <h2>タグ検索</h2>
      <table class="table table-hover table-inverse">
          <% @tag_list.each do |list|%>
            <i class="fa-sharp fa-solid fa-tag"></i>
            <%=link_to list.name, admin_search_tag_path(tag_id: list.id) %>
            <%="(#{list.post_pets.count})" %>
          <% end %>

        <thead>
          <tr>
            <th>ユーザー</th>
            <th>お気に入りの一枚</th>
            <th>タイトル</th>
            <th colspan="3"></th>
          </tr>
        </thead>

        <% @post_pets.each do |post_pet| %>
        <tbody>
          <tr>

            <td>
              <%= link_to admin_customer_path(post_pet.customer) do %>
                <%= image_tag post_pet.customer.get_profile_image, size:"100x100" %>
                <%= post_pet.customer.name %>
              <% end %>
            </td>


            <td>
              <%= link_to admin_post_pet_path(post_pet.id) do %>
                <%= image_tag post_pet.image, size: "200x180" %>
              <% end %>
            </td>

            <td>
              <%= link_to admin_post_pet_path(post_pet.id) do %>
                <%= post_pet.title %>
              <% end %>
            </td>

          </tr>
        </tbody>
        <% end %>

      </table>
    </div>
  </div>
</div>